---
title: Item Group
description: An item group that typically groups related information together.
apiReference: https://pub.dev/documentation/forui/latest/forui.widgets.item/
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Widget } from '@/components/demo/widget';
import { CodeSnippet } from '@/components/code-snippet/code-snippet';
import { UsageSnippet } from '@/components/usage-snippet/usage-snippet';
import defaultSnippet from '@/snippets/examples/data/item-group/default.json';
import scrollableSnippet from '@/snippets/examples/data/item-group/scrollable.json';
import lazySnippet from '@/snippets/examples/data/item-group/lazy.json';
import mergeSnippet from '@/snippets/examples/data/item-group/merge.json';
import fullSnippet from '@/snippets/examples/data/item-group/full.json';
import indentedSnippet from '@/snippets/examples/data/item-group/indented.json';
import itemGroupUsage from '@/snippets/usages/data/item_group/itemGroup.json';
import itemGroupBuilderUsage from '@/snippets/usages/data/item_group/builder.json';
import itemGroupMergeUsage from '@/snippets/usages/data/item_group/merge.json';

<Callout type="info">
    This widget is typically used to create more complex widgets rather than being used directly.
</Callout>

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='data/item-group'/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={defaultSnippet} />
  </Tab>
</Tabs>

## CLI

To generate a specific style for customization:

<Tabs items={['Item Group', 'Items', 'Item', 'Item Content']}>
    <Tab value="Item Group">
        ```shell copy
        dart run forui style create item-group
        ````
    </Tab>
    <Tab value="Items">
        ```shell copy
        dart run forui style create items
        ````
    </Tab>
    <Tab value="Item">
        ```shell copy
        dart run forui style create item
        ````
    </Tab>
    <Tab value="Item Content">
        ```shell copy
        dart run forui style create item-content
        ````
    </Tab>
</Tabs>

## Usage

### `FItemGroup(...)`
<UsageSnippet usage={itemGroupUsage} />

### `FItemGroup.builder(...)`
<UsageSnippet usage={itemGroupBuilderUsage} />

### `FItemGroup.merge(...)`
<UsageSnippet usage={itemGroupMergeUsage} />

## Examples

### Behavior

#### Scrollable

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='data/item-group' variant='scrollable' height={200}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={scrollableSnippet} />
  </Tab>
</Tabs>

#### Lazy Scrollable

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='data/item-group' variant='lazy' height={300}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={lazySnippet} />
  </Tab>
</Tabs>

#### Merge Multiple Groups

This function merges multiple `FItemGroupMixin`s into a single group. It is useful for representing a group with
several sections.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='data/item-group' variant='merge' height={300}/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={mergeSnippet} />
  </Tab>
</Tabs>

### Appearance

#### Full Divider

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='data/item-group' variant='full'/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={fullSnippet} />
  </Tab>
</Tabs>

#### Indented Divider

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <Widget name='data/item-group' variant='indented'/>
  </Tab>
  <Tab value="Code">
    <CodeSnippet snippet={indentedSnippet} />
  </Tab>
</Tabs>
