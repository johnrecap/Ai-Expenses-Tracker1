# Validation: 014 Production Backend Ops

Date: 2026-05-29

## Completed Checks

```powershell
cd server
cmd /c npm ci
cmd /c npm run typecheck
cmd /c npm test
```

Results:

- `npm ci`: passed after running outside the sandbox so npm could write to the
  user npm cache.
- `npm run typecheck`: passed.
- `npm test`: passed, 9 test files and 20 tests.

## Flutter Checks

```powershell
flutter analyze --no-pub
flutter test --no-pub
```

Results:

- `flutter analyze --no-pub`: failed on existing Flutter issues outside this
  backend-ops change.
- `flutter test --no-pub`: failed on existing Flutter test/API drift outside
  this backend-ops change.

Known blockers observed:

- `lib/app/router.dart` imports missing
  `features/expenses/presentation/add_expense_ai_text_screen.dart`.
- `test/app/*` still calls `AppRouter.create()` without the required
  `AuthBloc`.
- Several `test/blocs/*` files import old `lib/screens/...` paths that do not
  exist in the current feature-first structure.
- `test/features/onboarding/splash_screen_test.dart` builds widgets that expect
  an `AuthBloc` provider.
- `test/features/expenses/transaction_widgets_test.dart` creates `Category`
  without the required `totalExpenses` field.

The first analyzer run also scanned local reference repositories under
`docs/agent-playbooks/references/**`; `analysis_options.yaml` now excludes
those references so they stay in Git/documentation but do not count as app
source.

## Security And Scope Notes

- `firebase.json` does not include a Firebase Functions source.
- No `functions/` folder was added.
- `server/.env.example` uses placeholders only.
- `server/node_modules/` was generated by `npm ci` and is ignored by
  `.gitignore`.
- `npm ci` reported 16 moderate npm audit findings. No automatic
  `npm audit fix` was run because it can change dependency resolution broadly.
  `npm audit --audit-level=high` could not complete in the sandboxed terminal
  because the npm audit endpoint/cache write failed.
