name: expense_repository
description: "Repository interfaces and implementations for expenses, categories, budgets, and sync"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.17.5
  firebase_auth: ^4.16.0
  google_sign_in: ^6.1.6
  uuid: ^4.4.0
  equatable: ^2.0.5
  http: ^1.2.2
  drift: ^2.21.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.21.0
  build_runner: ^2.4.13
